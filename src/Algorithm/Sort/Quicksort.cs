namespace Algorithms;

internal static partial class Sort
{
    internal static int[] Quicksort(params int[] array)
    {
        if (array.Length < 2)
            return array;

        return array;
    }
}